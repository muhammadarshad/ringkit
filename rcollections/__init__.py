"""
ringkit.rcollections — ring-native data structures (placeholder; original name per D10 —
also avoids shadowing stdlib `collections` when Python runs with CWD inside the repo).

Home for containers built on ring semantics (as opposed to array/, which is the math side:
RingTensor + the rnp numpy surface). Charter disciplines apply: multiplier-free, no
standard-math imports, verify by execution.
"""
